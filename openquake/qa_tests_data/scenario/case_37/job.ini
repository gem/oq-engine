[general]
description = Test use of with_betw_ratio in job file when already using mgmpe wrapped GSIMs
calculation_mode = scenario
ses_seed = 42

[geometry]
site_model_file = site_model.csv

[logic_tree]
gsim_logic_tree_file = gsim_lt.xml

[rupture]
rupture_model_file = rupture.csv

[calculation]
intensity_measure_types = SA(0.3)
truncation_level = 3
maximum_distance = 300
number_of_ground_motion_fields = 10
with_betw_ratio = 1.7
