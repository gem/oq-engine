#!/bin/bash
# run this script as user openquake

if [ -z $1 ]; then
    echo "ERROR: please provide the path to the 'server/db/upgrade_manager.py' script"
    exit 1
fi

python $1 ~openquake/db.sqlite3
