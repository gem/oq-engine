[general]
description = Compute risk around a rupture
calculation_mode = scenario_risk
exposure_file = ../../../../exposure.hdf5
aggregate_by = NAME_1, OCCUPANCY; NAME_1; OCCUPANCY

[calculation]
rupture_dict = {'lon': 139, 'lat': 35, 'dep': 6, 'mag': 9.5, 'rake': 0}
truncation_level = 3.0
maximum_distance = 600
number_of_ground_motion_fields = 10
asset_hazard_distance = 50
tectonic_region_type = Active Shallow Crust
