sudo: false

language: python

python:
 - "2.7"

addons:
  apt:
    packages:
    - gfortran
    - liblapack-dev
    - python-numpy
    - python-scipy

install:
  - pip install psutil shapely h5py
  - python setup.py install

script:
  nosetests -a'!slow' -vx
