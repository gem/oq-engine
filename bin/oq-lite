#!/usr/bin/env python

import logging
from openquake.commonlib import sap, __version__
from openquake.lite.commands import run, info, check

if __name__ == '__main__':
    parser = sap.compose([check.parser, run.parser, info.parser],
                         version=__version__)
    parser.callfunc()
