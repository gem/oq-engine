[general]

description = Classical PSHA Risk Demo
calculation_mode = classical

vulnerability_file = vulnerability.xml
exposure_file = exposure.xml

lrem_steps_per_interval = 5


region_constraint = -150 90, 150 90, 150 -90, -150, -90

export_dir = /tmp/
